add_executable(test_core test_core.cpp)
target_link_libraries(test_core PRIVATE pf_test_common)
if(MSVC)
  target_compile_options(test_core PRIVATE /W4 /WX)
endif()
add_test(NAME test_core COMMAND test_core)

if(PREFILLFABRIC_HAS_CUDA)
  add_executable(test_cuda test_cuda.cpp)
  target_link_libraries(test_cuda PRIVATE pf_test_common)
  if(MSVC)
    target_compile_options(test_cuda PRIVATE /W4 /WX)
  endif()
  add_test(NAME test_cuda COMMAND test_cuda)
endif()
